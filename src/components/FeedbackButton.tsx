import React from "react";

const FeedbackButton = () => {
  // This component has been replaced by UniversalFeedbackSystem
  // Keeping the component structure to avoid breaking imports
  return null;
};

export default FeedbackButton;
